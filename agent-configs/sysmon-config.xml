<Sysmon schemaversion="4.90">

  <HashAlgorithms>SHA256</HashAlgorithms>

  <EventFiltering>

    <!-- Process Creation — only PowerShell to reduce volume -->
    <ProcessCreate onmatch="include">
      <Image condition="contains">powershell.exe</Image>
    </ProcessCreate>

    <!-- Network Connections — flag common C2/reverse-shell ports -->
    <NetworkConnect onmatch="include">
      <DestinationPort condition="is">4444</DestinationPort>
      <DestinationPort condition="is">1337</DestinationPort>
    </NetworkConnect>

    <!-- File Creation -->
    <FileCreate onmatch="include" />

    <!-- Registry Changes -->
    <RegistryEvent onmatch="include" />

    <!-- Driver Loads -->
    <DriverLoad onmatch="include" />

    <!-- DNS Queries -->
    <DnsQuery onmatch="include" />

    <!-- Process Access — LSASS credential dumping (T1003.001) -->
    <ProcessAccess onmatch="include">
      <TargetImage condition="is">C:\Windows\System32\lsass.exe</TargetImage>
    </ProcessAccess>

  </EventFiltering>

</Sysmon>